RewriteEngine On
RewriteBase /notes/
# Redirect root requests to public/
RewriteRule ^$ public/ [L]
# Forward other requests to public/
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ public/$1 [L]
